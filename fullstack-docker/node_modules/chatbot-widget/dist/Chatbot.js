import React from 'react';
import { Provider } from 'react-redux';
import { store } from './store';
import ChatbotUI from './ChatbotUI';
import './Chatbot.css';
export default function Chatbot({ apiUrl, avatarUrl }) {
    // Imposta dinamicamente la base URL nel contesto globale (accessibile da rasaApi)
    window.__CHATBOT_API_URL__ = apiUrl;
    return (React.createElement(Provider, { store: store },
        React.createElement(ChatbotUI, { avatarUrl: avatarUrl })));
}
