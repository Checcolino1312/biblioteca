import React from 'react';
import './Chatbot.css';
type ChatbotProps = {
    apiUrl: string;
    avatarUrl?: string;
};
export default function Chatbot({ apiUrl, avatarUrl }: ChatbotProps): React.JSX.Element;
export {};
